{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ],
  "//": "core/ and shared/ are device-independent logic with their own typecheck config (tools/test/tsconfig.json); they use .ts-extension imports and node:test, so they are excluded from the app's tsc pass.",
  "exclude": ["node_modules", "core", "shared", "tools", "mock-api"]
}
