{
  "name": "saksi-tools-test",
  "private": true,
  "type": "module",
  "description": "Optional richer test/typecheck harness for lane A logic. The zero-install fallback is `node tools/run-tests.mjs`.",
  "scripts": {
    "test": "vitest run --root ../..",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
